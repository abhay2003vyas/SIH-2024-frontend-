import React from "react";
import "./DetectAISection.css";

const DetectAISection = () => {
  return (
    <section className="detect-ai-section">
      <div className="content">
        <h1 className="headline">Detect AI generated videos at scale</h1>
        <p className="subheadline">
          Our AI video detection automatically detects videos from popular AI
          generators. No watermarks needed.
        </p>
      </div>
    </section>
  );
};

export default DetectAISection;
